fileManager
  maximale uploadgröße für bilder
  

siteeditor
  build fields
  select apiload
  list Menu
  teaser
  teasergroup
  Header
  formeditor

renderer:
  blocks
  listplugins
  siteconstants
  .length
  if <,>,!=

theme
  delete parts on delete

pluginEditor
  sync button
  responsive

themeEditor
  sync button
  select for site or newsletter
  responsive

quill:
var toolbarOptions = [
  ['bold', 'italic', 'underline', 'strike'],        // toggled buttons
  ['blockquote', 'code-block'],

  [{ 'list': 'ordered'}, { 'list': 'bullet' }],
  [{ 'header': [1, 2, 3, 4, 5, 6, false] }],

  [{ 'align': [] }]
];

var quill = new Quill('#editor-container', {
  modules: {
    toolbar: toolbarOptions
  },
  theme: 'snow'
});
